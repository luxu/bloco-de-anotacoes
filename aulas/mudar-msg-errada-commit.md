# Para mudar o texto errado
```git commit --amend``` (Muda a mensagem do último commit)
Após digita o comando `git commit --amend` será aberto o último commit no editor de texto (nano) e você pode editar, salvar com CTRL+X e ver a alteração com o comando `git log`
